# pdf_form_sniffer
Finds text boxes and buttons in PDF fillable forms and overlays them with XY coordinates

Download a nice (un)friendly fillable form (like IRS form 8621) and run the script to get the coordinates of textboxes and buttons 
